<%= simple_form_for(@bug, html: { class: "form-horizontal" } ) do |f| %>
  <% if @bug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug.errors.count, "error") %> prohibited this bug from being saved:</h2>

      <ul>
      <% @bug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.input :user_id, collection: User.all, label_method: :first_name %>

    <%= f.input :priority_id, collection: Priority.all, label_method: :name %>

    <%= f.input :status_id, collection: Status.all, label_method: :name %>

    <%= f.input :description, :input_html => { :rows => 5 } %>

    <%= f.input :details, :input_html => { :rows => 5 } %>

    <div class="form-actions">
        <%= f.button :submit %>
    </div>

<% end %>
